{namespace prezentacja.templates.Table}

/**
* @param data
*/
{template .table}
    {foreach $row in $data}
    <tr class="{if index($row) % 2 == 0}even{else}odd{/if}">
        {call .renderrow}
            {param row: $row /}
        {/call}
    </tr>
    {/foreach}
{/template}

/**
* @param row
*/
{template .renderrow private="true"}
    <td>{$row.name}</td>
    <td>{$row.address}</td>
    <td>
        <a href="{$row.www}">
            <img src="/img/icons/icon_vcard.gif" alt="{$row.www}">
            {$row.www}
        </a>
    </td>
    <td>
        <a href="mailto:{$row.email}">
            <img src="/img/icons/icon_mailto.gif" alt="{$row.email}">
            {$row.email}
        </a>
    </td>
    <td>{$row.leg_count}</td>
    <td>
        <a href="#">
            <img src="/img/icons/rosette.png" alt="foo">
        </a>
        <a href="#">
            <img src="/img/icons/tick.png" alt="bar">
        </a>
        <a href="#">
            <img src="/img/icons/icon_plugin.gif" alt="baz">
        </a>
        <a href="#">
            <img src="/img/icons/cog.png" alt="bum">
        </a>
    </td>
{/template}
